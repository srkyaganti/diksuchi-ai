# setup venv
python3 -m venv indic-parler-tts

# activate venv
source parler/bin/activate

for windows I had to do .\indic-parler-tts\Scripts\Activate.ps1 in powershell

I had to install torch for cuda


# deactivate venv if required
deactivate

# install dependencies
pip install -r requirements-server.txt

# start the server
uvicorn server:app --host 0.0.0.0 --port 8000


I had to install torch for cuda 

pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu130
I needed to install cuda first from nvidia cuda toolkit installer

I also needed installing parler tts pip3 install git+https://github.com/huggingface/parler-tts.git